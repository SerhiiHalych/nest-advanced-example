export enum TwilioMessageStatus {
  accepted = 'accepted',
  scheduled = 'scheduled',
  queued = 'queued',
  sending = 'sending',
  sent = 'sent',
  receiving = 'receiving',
  received = 'received',
  delivered = 'delivered',
  undelivered = 'undelivered',
  failed = 'failed',
  read = 'read',
  canceled = 'canceled',
}
